# 📊 Каталог данных для слоя Gold

## 🔎 Обзор

**Gold Layer** — это бизнес-ориентированное представление данных, структурированное для поддержки аналитики и отчетности.  
Он состоит из **измерений (dimension tables)** и **фактовых таблиц (fact tables)**, где хранятся ключевые бизнес-метрики.  

---

## 🧑‍🤝‍🧑 `gold.dim_customers`

**Назначение:** хранит данные о клиентах, дополненные демографической и географической информацией.  

| 🏷️ Название столбца | 🔢 Тип данных  | 📖 Описание |
|---------------------|---------------|-------------|
| `customer_key`      | int           | Суррогатный ключ, уникально идентифицирующий запись клиента. |
| `customer_id`       | int           | Уникальный числовой идентификатор клиента. |
| `customer_number`   | nvarchar(50)  | Алфавитно-цифровой идентификатор клиента. |
| `first_name`        | nvarchar(50)  | Имя клиента. |
| `last_name`         | nvarchar(50)  | Фамилия клиента. |
| `country`           | nvarchar(50)  | Страна проживания клиента (например, `Australia`). |
| `marital_status`    | nvarchar(50)  | Семейное положение (`Married`, `Single`). |
| `gender`            | nvarchar(50)  | Пол клиента (`Male`, `Female`, `n/a`). |
| `birthdate`         | date          | Дата рождения в формате `YYYY-MM-DD` (например, `1971-10-06`). |
| `create_date`       | date          | Дата создания записи клиента в системе. |

---

## 📦 `gold.dim_products`

**Назначение:** хранит информацию о продуктах и их характеристиках.  

| 🏷️ Название столбца    | 🔢 Тип данных  | 📖 Описание |
|------------------------|---------------|-------------|
| `product_key`          | int           | Суррогатный ключ продукта. |
| `product_id`           | int           | Уникальный идентификатор продукта. |
| `product_number`       | nvarchar(50)  | Код продукта (для учета/категоризации). |
| `product_name`         | nvarchar(50)  | Название продукта с деталями (тип, цвет, размер). |
| `category_id`          | nvarchar(50)  | Уникальный идентификатор категории продукта. |
| `category`             | nvarchar(50)  | Общая категория (`Bikes`, `Components`). |
| `subcategory`          | nvarchar(50)  | Подкатегория продукта. |
| `maintenance_required` | nvarchar(50)  | Требуется ли обслуживание (`Yes`, `No`). |
| `cost`                 | int           | Стоимость (базовая цена). |
| `product_line`         | nvarchar(50)  | Продуктовая линейка (`Road`, `Mountain`). |
| `start_date`           | date          | Дата, когда продукт стал доступен для продажи. |

---

## 📈 `gold.fact_sales`

**Назначение:** хранит транзакционные данные о продажах для аналитики.  

| 🏷️ Название столбца | 🔢 Тип данных  | 📖 Описание |
|---------------------|---------------|-------------|
| `order_number`      | nvarchar(50)  | Уникальный номер заказа (например, `SO54496`). |
| `product_key`       | int           | Суррогатный ключ, связывающий заказ с продуктом. |
| `customer_key`      | int           | Суррогатный ключ, связывающий заказ с клиентом. |
| `order_date`        | date          | Дата оформления заказа. |
| `shipping_date`     | date          | Дата отгрузки заказа клиенту. |
| `due_date`          | date          | Дата, к которой должен быть произведен платеж. |
| `sales_amount`      | int           | Общая сумма продажи по позиции заказа. |
| `quantity`          | int           | Количество единиц товара в заказе. |
| `price`             | int           | Цена за единицу товара. |

---



Каталог данных для слоя Gold
Обзор
Gold Layer — это бизнес-ориентированное представление данных, структурированное для поддержки аналитики и отчетности. Он состоит из измерений (dimension tables) и фактовых таблиц (fact tables), в которых хранятся ключевые бизнес-метрики.
1. gold.dim_customers
Назначение: хранит данные о клиентах, дополненные демографической и географической информацией.
Название столбца	Тип данных	Описание
customer_key	int	Суррогатный ключ, уникально идентифицирующий запись клиента в таблице измерений.
customer_id	int	Уникальный числовой идентификатор клиента.
customer_number	nvarchar(50)	Алфавитно-цифровой идентификатор клиента для отслеживания и ссылок.
first_name	nvarchar(50)	Имя клиента.
last_name	nvarchar(50)	Фамилия клиента.
country	nvarchar(50)	Страна проживания клиента (например, "Australia").
marital_status	nvarchar(50)	Семейное положение клиента ("Married", "Single" и т. д.).
gender	nvarchar(50)	Пол клиента ("Male", "Female", "n/a").
birthdate	date	Дата рождения клиента в формате YYYY-MM-DD (например, 1971-10-06).
create_date	date	Дата создания записи клиента в системе.
2. gold.dim_products
Назначение: хранит информацию о продуктах и их характеристиках.
Название столбца	Тип данных	Описание
product_key	int	Суррогатный ключ, уникально идентифицирующий запись о продукте.
product_id	int	Уникальный идентификатор продукта для внутреннего учета.
product_number	nvarchar(50)	Структурированный код продукта (используется для учета или классификации).
product_name	nvarchar(50)	Название продукта с деталями (тип, цвет, размер и т. д.).
category_id	nvarchar(50)	Уникальный идентификатор категории продукта.
category	nvarchar(50)	Общая категория продукта (например, "Bikes", "Components").
subcategory	nvarchar(50)	Более детализированная подкатегория продукта.
maintenance_required	nvarchar(50)	Признак необходимости обслуживания ("Yes", "No").
cost	int	Стоимость (базовая цена) продукта.
product_line	nvarchar(50)	Продуктовая линейка (например, "Road", "Mountain").
start_date	date	Дата, когда продукт стал доступен для продажи или использования.
3. gold.fact_sales
Назначение: хранит транзакционные данные о продажах для аналитики.
Название столбца	Тип данных	Описание
order_number	nvarchar(50)	Уникальный идентификатор заказа (например, "SO54496").
product_key	int	Суррогатный ключ, связывающий заказ с таблицей продуктов.
customer_key	int	Суррогатный ключ, связывающий заказ с таблицей клиентов.
order_date	date	Дата оформления заказа.
shipping_date	date	Дата отгрузки заказа клиенту.
due_date	date	Дата, к которой должен быть произведен платеж.
sales_amount	int	Общая сумма продажи по позиции заказа.
quantity	int	Количество единиц продукта в заказе.
price	int	Цена за единицу товара.
